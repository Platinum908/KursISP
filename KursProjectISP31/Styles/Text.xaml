<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    
    <!-- Navigation Text Style -->
    <Style x:Key="Text_Style" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="Segoe UI" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="HorizontalAlignment" Value="Left" />
        <Setter Property="Foreground" Value="White" />
        <Setter Property="FontSize" Value="16" />
        <Setter Property="FontWeight" Value="Medium" />
        <Setter Property="Margin" Value="15,0,0,0" />
    </Style>

    <!-- Page Title Style -->
    <Style x:Key="PageTitleStyle" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="Segoe UI" />
        <Setter Property="FontSize" Value="28" />
        <Setter Property="FontWeight" Value="Bold" />
        <Setter Property="Foreground" Value="#2c3e50" />
        <Setter Property="Margin" Value="0,0,0,20" />
        <Setter Property="HorizontalAlignment" Value="Center" />
    </Style>

    <!-- Section Title Style -->
    <Style x:Key="SectionTitleStyle" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="Segoe UI" />
        <Setter Property="FontSize" Value="20" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Foreground" Value="#34495e" />
        <Setter Property="Margin" Value="0,0,0,15" />
    </Style>

    <!-- Label Style -->
    <Style x:Key="LabelStyle" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="Segoe UI" />
        <Setter Property="FontSize" Value="14" />
        <Setter Property="FontWeight" Value="Medium" />
        <Setter Property="Foreground" Value="#2c3e50" />
        <Setter Property="Margin" Value="0,0,0,5" />
    </Style>

    <!-- Data Text Style -->
    <Style x:Key="DataTextStyle" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="Segoe UI" />
        <Setter Property="FontSize" Value="14" />
        <Setter Property="Foreground" Value="#34495e" />
        <Setter Property="VerticalAlignment" Value="Center" />
    </Style>

    <!-- Status Text Style -->
    <Style x:Key="StatusTextStyle" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="Segoe UI" />
        <Setter Property="FontSize" Value="12" />
        <Setter Property="FontWeight" Value="Medium" />
        <Setter Property="Padding" Value="8,4" />
        <Setter Property="Background" Value="#ecf0f1" />
        <Setter Property="Foreground" Value="#2c3e50" />
        <Setter Property="CornerRadius" Value="4" />
    </Style>

    <!-- Success Status Style -->
    <Style x:Key="SuccessStatusStyle" TargetType="TextBlock" BasedOn="{StaticResource StatusTextStyle}">
        <Setter Property="Background" Value="#d5f4e6" />
        <Setter Property="Foreground" Value="#27ae60" />
    </Style>

    <!-- Warning Status Style -->
    <Style x:Key="WarningStatusStyle" TargetType="TextBlock" BasedOn="{StaticResource StatusTextStyle}">
        <Setter Property="Background" Value="#fef9e7" />
        <Setter Property="Foreground" Value="#f39c12" />
    </Style>

    <!-- Error Status Style -->
    <Style x:Key="ErrorStatusStyle" TargetType="TextBlock" BasedOn="{StaticResource StatusTextStyle}">
        <Setter Property="Background" Value="#fadbd8" />
        <Setter Property="Foreground" Value="#e74c3c" />
    </Style>

</ResourceDictionary>